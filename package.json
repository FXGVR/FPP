{
  "name": "fpp",
  "private": true,
  "version": "1.2.8",
  "description": "FXG Pano Player",
  "scripts": {
    "build": "node build-fp.js",
    "build:gzip": "node build-fp.js",
    "check:build": "node build-fp.js --check",
    "check": "node --check app.js && node --check build-fp.js && node build-fp.js --check"
  }
}
